# dotfiles

## Prerequisites

1. Install Homebrew.
2. Install Ansible (`brew install ansible`).

## Installing

```
cd ~
git clone https://github.com/codahale/dotfiles.git .dotfiles
cd ~/.dotfiles
make
```
